{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panier.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ panier.date ? panier.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ panier.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mon_panier_index') }}">back to list</a>

    <a href="{{ path('app_mon_panier_edit', {'id': panier.id}) }}">edit</a>

    {{ include('mon_panier/_delete_form.html.twig') }}
{% endblock %}
