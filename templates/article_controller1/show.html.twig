{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ article.Name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ article.image }}</td>
            </tr>
            <tr>
                <th>Remise</th>
                <td>{{ article.remise }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ article.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_controller1_index') }}">back to list</a>

    <a href="{{ path('app_article_controller1_edit', {'id': article.id}) }}">edit</a>

    {{ include('article_controller1/_delete_form.html.twig') }}
{% endblock %}
