{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user.Name }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ user.LastName }}</td>
            </tr>
            <tr>
                <th>Phone_Number</th>
                <td>{{ user.PhoneNumber }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ user.imageName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mon_user_index') }}">back to list</a>

    <a href="{{ path('app_mon_user_edit', {'id': user.id}) }}">edit</a>

    {{ include('mon_user/_delete_form.html.twig') }}
{% endblock %}
